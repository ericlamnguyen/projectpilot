# A project using Vite + React + TS + React Query + React Router

## Spin up the backend

git clone https://github.com/ericlamnguyen/projectpilot_backend.git

## Build and serve the server

```bash
npm install -g serve
npm install
npm run check
npm run build
serve -s dist
```
